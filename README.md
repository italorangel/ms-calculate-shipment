# ms-calculate-shipment
Projeto para o processo seletivo da Kabum

Para rodar o projeto usando o docker

```
docker-compose up --build
```

ou se você já tiver o ambiente pronto:
```
./entrypoint.sh
```

para rodar os testes
```
pytest
```
